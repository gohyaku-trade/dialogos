# XからDialogosへ：公開確認と共有

## 現状（2026-09-21）

X向けの共有導線と14人格の調整はローカルの公開用コードで準備しています。**本番への反映・本番URLの発行は未完了**です。

- 接続中のVercelチーム `filo-s-projects1` のプロジェクト一覧は0件。
- 販売版フォルダに `.vercel/project.json` はなく、ローカルVercel CLIも未ログイン。
- 本番ドメイン・本番DB移行・Turnstile設定・環境変数は未確認。
- 画面確認用の `http://127.0.0.1:15180/` は、このPC内だけの固定返答デモです。Xに貼ったり公開したりしません。

既存の公開URLまたはVercelプロジェクト画面URLから公開対象を特定します。未公開ならその旨を確認してから、意図したアカウントに作成します。アクセスできない既存プロジェクトの代わりに別プロジェクトを勝手に作成しません。

## 公開後に貼るリンク

確認した本番HTTPSのトップURLをそのままXへ貼ります。短縮サービスや中継ページは不要です。

賢者を指定する場合は、その本番URLの末尾へ `?sage=socrates` などを付けます。14人の既知IDだけを受け付け、未知の指定は一覧へ戻します。リンクを開いただけでは対話を送信せず、Googleログインも要求しません。無料対話を開始・送信する操作ではボット判定が必要です。

本番URLが正しく設定された公開ビルドでは「Xで紹介」から投稿作成画面を開けます。投稿ボタンを押すのは利用者本人です。共有URLへ会話・利用者名・認証情報・元ページの任意クエリを埋め込みません。

## 公開前チェック

1. `DEPLOYMENT.md` と `ANONYMOUS_TRIAL.md` に従い、DBの003〜006、実Turnstile、署名Cookie、予算停止を検証します。これらが未設定なら無料対話は停止し、人格の持ち帰りだけ利用可能です。
2. 確定した本番URLをVercelのProduction用 `APP_URL` に設定してビルドします。固定カード画像とOG/Xメタタグは静的HTMLへ出すため、カード取得にAI APIは不要です。ビルド設定を変えずローカルソースのindex.htmlだけをアップロードしないでください。
3. 未ログインの外部ブラウザとスマホのXアプリ内ブラウザから、本番URLと賢者指定リンクを確認します。Vercelのログイン画面・保護画面で止まらないことが必要です。一時的な保護回避URLを共有して解決しません。
4. 本番のトップHTML・カード画像・必要なJS/CSSが200、秘密ファイルやSQLが404であることを確認します。カード画像の取得を無差別なボット遮断で止めない一方、生成APIの防御は維持します。
5. 初回表示→任意の賢者で3往復→3回目の返答表示→人格コピーの一連を確認します。4回目・別タブ・再読み込み・回線上限・全体無料予算停止も確認します。
6. Xカードの実表示はX側の取得・キャッシュにも依存します。OGの設定だけで即時表示を保証しません。公開確認後、実際の投稿プレビューを確認してから告知します。

## 告知文のたたき台

> ソクラテス、ニーチェ、ブッダ……同じ問いを、違う思想で考えてみる。
> 14の賢者と対話できる「Dialogos」を作りました。
> ログイン不要で、好きな賢者と合計1日3往復お試し。
> 気に入った人格は無料でコピーして、自分のChatGPTへ。
> ［確認済みの本番URLをここに入れる］

無料対話を本番で有効化・確認できてからこの文面を使用します。予算上限・混雑等でお試しを休止する場合があるため、「誰でも必ず毎日3往復」を保証しません。歴史上の本人や実際の啓示ではなく、思想に着想を得たAIです。

## 費用と公開範囲

モデルはgpt-5.4-miniのまま。無料API予算は日次0.50ドル・月次5ドル、全体は日次3ドル・月次30ドルのままです。無料サブ予算は全体予算の内側です。告知規模を増やしても自動増額はしません。無料の人格持ち帰りはAI APIを使いませんが、配信元の上限・停止は別に影響します。

Xへの自動投稿、ドメイン購入、Vercel有料プランへの変更、既存契約の解約、予算増額はこの変更に含みません。
