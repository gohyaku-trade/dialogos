# 人格持ち帰り戦略と実装範囲

## 目的と導線

「人格設計を自分でする手間が省ける」価値を入口にします。

1. X等から賢者一覧へ来る。
2. 14人から選び、ログインせず人格設定を確認・コピーする。またはボット判定を経て、Googleログインせず好きな賢者と無料対話する。
3. アプリの無料対話は匿名ブラウザごとに、全14人の賢者で合計1日3往復、日本時間0時に更新。3往復後も最後の回答を読める。既存Googleアカウントの対話機能も残す。
4. 人格をコピーし、自分でChatGPTを開き、新しいチャットに貼り付けて送信する。

コピーは無料・無制限です。体験完了・ログイン・購入・フォローを条件にしません。賢者カード、対話画面、無料枠終了時から到達できます。予算・認証・API障害時も静的な人格持ち帰りは利用できます。

新しいChatGPTチャットへ指示文を貼る方式であり、GPTsの自動作成やアカウントへの自動インストールではありません。ChatGPTでの利用可否・上限は利用者側の条件に従います。[ChatGPTのプロンプト](https://learn.chatgpt.com/docs/prompting)

## プライバシー

- 初期状態は人格設定だけ。ユーザーの会話・内部メモ・認証情報を含めない。
- 会話を含めるチェックは初期OFF。本人が選んだ場合のみ、現在表示中のユーザー／賢者の発言を含める。
- コピー前に全文を確認できる。長い会話の省略は画面・出力に示す。
- コピーに失敗した場合は成功表示を出さず、選択・手動コピーを案内する。
- ChatGPTの固定URLを利用者の操作で開く。本文・個人情報をURLに入れず、自動送信しない。
- 貼り付けた後の情報はChatGPT側の設定・規約に従う。秘密や他者の個人情報を含めないよう案内する。

## 指標の定義と未接続部分

`js/services/metrics.js` は、固定のイベント名・賢者ID・入口だけを扱います。

| イベント | 実際に確認できる操作 |
|---|---|
| takeaway_open | 人格プレビューを開いた |
| takeaway_copy_success | ブラウザのクリップボード書き込みが成功した |
| takeaway_manual_copy | 手動コピーの案内が必要になった（コピー完了ではない） |
| takeaway_chatgpt_open | ChatGPTへのリンクを押した（貼り付け・利用開始ではない） |

現時点ではページ内メモリの診断カウンタと `dialogos:takeaway` CustomEventだけです。再読込でリセットされ、サイト全体の集計・ユニーク人数・Xのフォロー獲得数は収集していません。外部送信・永続ID・Cookie追加はありません。開発者コンソールで `import('./js/services/metrics.js').then(m => console.table(m.getTakeawayMetrics()))` により、そのページ内の動作だけ確認できます。

本番の全体集計を開始する場合は、利用プランと費用、最小限の収集項目、プライバシー案内、同意要否を確認してから収集先を接続します。会話本文・メール・アカウントID・完全URLは送信しない設計を維持します。コピーとリンク押下から実際のChatGPTでの使用を推定して断定してはいけません。

## 初期運用

- コピー導線の利用を優先し、無料対話のAPI予算は現状維持する。
- X投稿は「同じ問いに対する賢者の違い」と「人格を無料で持ち帰れる」を明記する。歴史上の本人の発言として扱わない。
- まずスマホでコピー手順が迷わず完了するか確認する。広告・フォロー強制で入口を塞がない。
- 公開後に全体計測が接続できたら、入口別のコピー成功率と手動コピー発生率で導線を改善する。
- API予算の増額、広告・寄付・協賛、課金再開、ホスティング移行は今回の変更に含めない。

## 公開に必要な作業

ローカルコードの変更だけでは本番に反映されません。`003`→`004`→`005_daily_trial.sql`→`006_anonymous_trial.sql` の移行を別DBで検証し、`SALES_ENABLED=false` を設定してデプロイします。匿名試用はTurnstileの本番キー・署名用秘密鍵・正しいAPP_URLを設定し、`TRIAL_ENABLED=true` と `ANONYMOUS_TRIAL_ENABLED=true` を検証後に有効化。設定不足では生成しません。既存利用者の残高・履歴・契約は照合し、変更案内を用意します。詳細は `DEPLOYMENT.md` と `ANONYMOUS_TRIAL.md`。
