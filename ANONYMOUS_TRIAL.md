# ログイン不要のお試し：安全設計と公開手順

## 提供するもの

好きな賢者で合計1日3往復、午前0時（Asia/Tokyo）更新。匿名Cookieで識別したブラウザ単位です。全14人格の閲覧・持ち帰りは、Cookie・ログイン・ボット判定なしで無制限に利用できます。

匿名利用を実在する「1人」と同定しません。Cookie削除・別ブラウザ・別回線による取り直し、高度なボット、共有回線の誤判定を完全には排除できません。Google利用者と匿名利用者の無料枠も自動統合しません。

## 防御の層

1. **明示操作まで作らない**：一覧閲覧・コピーでは匿名セッションを作らず、ボット判定もしません。セッションCookieの発行だけではDBの利用者行を作らず、最初の正常な生成予約で作ります。
2. **ボット判定**：Turnstileをサーバーで検証。開始時は `guest_session`、新規送信時は `guest_chat` と送信UUIDを照合。ホスト名・有効期限も検証し、未設定・失敗・タイムアウトは拒否。クライアントの「認証成功」だけでは信用しません。同じ送信UUIDの結果再確認は新たな生成をしません。
3. **署名付きCookie**：サーバーだけが発行するUUIDをHMACで署名。HTTPSでは `__Host-dialogos_guest`、HttpOnly・Secure・SameSite=Strict・Path=/、最長30日。JavaScriptが送った任意のゲストIDでは認証しません。HTTP例外はループバック開発用の別Cookieだけです。
4. **回数と連打制限**：DBの原子的な予約で匿名ブラウザごとに1日3往復、1件だけ生成中。さらに回線ごとに日本時間1日30新規予約・直近1分6新規予約。これは利用者向け無料回数とは別の不正利用防止上限で、生成失敗でも回線の試行数は戻しません。
5. **予算予約**：無料の日次0.50ドル・月次5ドル、および全体の日次3ドル・月次30ドルの既存上限を共有。生成前に最大費用を予約し、結果不明もゼロ円扱いしません。日次回数は日本時間、費用期間はUTCです。値上げはしていません。
6. **限定権限**：匿名経路は無料対話だけ。Googleアカウントの購入分・契約・履歴・呼び名変更へアクセスできません。ログイン切替時は以前の表示内容を破棄し、遅延応答も所有者を照合します。

回線識別には、Vercel実行環境でのみVercelの `x-vercel-forwarded-for` を単一IPとして使います。通常の開発サーバーはsocketの接続元だけを使い、任意の転送ヘッダーは信用しません。IPv6は/64へまとめ、秘密鍵と用途識別子でHMAC化。制限台帳に生IPを保存しません。Cloudflare等の別のリバースプロキシを前段に追加する場合は、この信頼境界と共有IPによる誤制限を再評価してください。

## 本番で必要な設定（このコード変更だけでは有効にならない）

1. 別のSupabase DBで既存 `003` → `004` → `005` → `006_anonymous_trial.sql` の追加移行を検証。既存残高・契約・予約中のリクエストを照合します。適用済みSQLを書き換えません。
2. Cloudflareで本番ドメインに限定したTurnstile widgetを作成します。サイト全体をCloudflareへ移す必要はありません。これは運営者による外部サービスの設定作業です。
3. 本番環境の `APP_URL` を固定HTTPS URLに設定。`TURNSTILE_SITE_KEY` は公開可、`TURNSTILE_SECRET_KEY` と `GUEST_SESSION_SECRET` はサーバー専用。後者は暗号学的乱数で32バイト以上を用意し、他のAPIキーと使い回しません。秘密値をチャット・Git・公開JSへ貼らないでください。
4. Turnstileのテストキーはローカル試験専用。本番で使用しないでください。検証バイパス用の本番フラグはありません。
5. `SALES_ENABLED=false` と既存の予算上限を維持してプレビューへデプロイ。キーがない状態で匿名生成が停止し、人格コピーが使えることも確認します。
6. 本番ドメインで確認後、`TRIAL_ENABLED=true` と `ANONYMOUS_TRIAL_ENABLED=true` を設定して再デプロイ。有料対話の `BILLING_ENABLED` は既存契約の検証後に別管理します。

`.env.example` は項目見本で、実際の秘密値は含みません。ローカルExpressはdotenvの `.env` を読み、Next.jsの `.env.local` 自動読込規則は適用されません。

Cookie署名鍵の変更は既存匿名セッションを無効にします。匿名DB履歴の自動削除は未導入です。Cookieの30日期限とDBの保管期間を混同せず、公開前に保存期間と削除運用を決めてください。課金・費用予約台帳はむやみに削除しないでください。

## DDoS・無駄なアクセスについて

これらは特にAIの無制限生成を抑えるもので、Functions実行、配信、DB、認証等の費用やサービス停止を完全に防ぐものではありません。外部から正当な形式のリクエストを大量に送られる余地は残ります。

Vercel Firewallで `/api/guest/session` と `/api/guest/chat` のPOSTをまずログ監視し、実際の正常利用・共有回線の状況を確認してからレート制限を検討します。`/api/stripe/webhook` はこのチャレンジ対象にしません。ログ → 運営者確認 → プレビュー遮断試験 → 運営者が本番公開、の順です。今回のローカル変更でFirewallルールを公開したり、契約プランを変更したりしていません。

緊急停止は `ANONYMOUS_TRIAL_ENABLED=false`。無料全体は `TRIAL_ENABLED=false`。既存購入者向け機能や人格コピーは別に維持できます。ただし配信元自体が停止した場合、新規訪問者が静的ページを開ける保証はありません。

公式根拠：

- [Turnstileのサーバー検証・単回トークン・5分の期限](https://developers.cloudflare.com/turnstile/get-started/server-side-validation/)
- [Vercelのリクエストヘッダーと転送IP](https://vercel.com/docs/headers/request-headers)
- [Vercel Firewall](https://vercel.com/docs/vercel-firewall)
