# v3 検証記録 — 2026-09-21

## 現行：X共有導線・14人格の最終微調整

Xの公開リンク → 一覧または指定した賢者のお試し入口 → 明示的な操作 → 人格コピー、という流れを確認。公開先の特定・本番接続は未完了のため、以下はローカルでの検証です。

`npm test` は **178件成功、失敗・skipなし**。`npm run check` と `npm run build` も成功。

- 14人格の価値・根拠・手法・反論対応・口調・境界を点検し、思想の混同や質問だけの引き延ばしを抑える微調整。APIと持ち帰り人格を同じ正典から再生成し一致を確認。原典・変更点・限界は `PERSONA_REVIEW.md`。
- UIの冒頭文は全14人とも「思想に着想を得た創作」と表示。名言の真正な引用のように見える旧表記を修正。
- `?sage=` は既知14IDだけを採用。余分なクエリから会話・認証情報を共有URLへ転送しない。旧決済処理を優先し、`?route=cancel` も互換維持。
- 共有URLはビルド時の明示的な公開 `APP_URL` と賢者IDだけ。Xの固定投稿作成リンクを表示し、本人のクリック／投稿操作を必要とする。クリップボード拒否時は手動選択へ切り替え、偽の成功表示はしない。
- canonical・OG・Xの画像URLは静的HTMLへ生成。未設定・ローカルURLは省略、不正な公開URLはビルド前に拒否。試験用canonicalで検証後、通常の未設定ビルドへ戻し、架空ドメインは納品物へ残さない。
- X用JPEGは1200×630、53,894 bytes。EXIF/XMPなし。カード表示にAI・画像変換APIは不要。X側の実カード取得・キャッシュ反映は本番公開後に確認が必要。
- `.vercelignore` の `data/` が `js/data/` まで除外する公開不具合を修正。root `/data/` のみ除外し、隔離Gitを用いたパッケージ回帰試験を追加。

Chromeの1264×625／390×844で画面確認。共有リンクのコピー成功、賢者への直接遷移、受付休止中の人格コピー成功、無効IDの一覧表示を確認。初回は `/api/config` のみで匿名作成・生成API・外部Google/Turnstile SDKを呼ばず、横はみ出し・ブラウザ例外・コンソールエラーはなし。匿名受付有効の入口は既存の固定返答モックでも確認。Xへの遷移／投稿、ChatGPTへの本文送信は行っていません。

**今回の人格変更を実モデルへ送信して品質採点したわけではありません。** オフラインの指示・契約の回帰試験と設計レビューです。下方の「実モデル」欄は以前の構成の過去記録で、今回の微調整後の実測ではありません。モデル・トークン上限・日次3往復・費用予算・課金ロジックは変更していません。

実OpenAI/Stripe課金、実Turnstile、クラウドDB移行、Vercel公開・Firewall更新は未実施。公開先が確定していないため、Xへ貼れる本番URLはまだ提供していません。公開条件は `X_LAUNCH.md`。

## 前回：画像軽量化・Googleログイン不要のお試し

利用者が賢者を選ぶ → 明示的にお試し開始 → ボット判定と署名Cookie → 匿名API → DBで回数・回線・費用を予約 → 返答保存 → 残数を表示、という流れを検証します。人格の閲覧・コピーはこの経路を必要としません。

当時の `npm test` は160件成功、失敗・skipなし。`npm run check` と `npm run build` も成功。バックエンドはHTTP→実SQL互換環境→模擬AI、ブラウザは模擬API/Turnstileで検証しており、本番接続での通し試験とは区別します。

- 元PNG14枚33,059,274 bytesを保持。WebPは128幅52,104 bytes／320幅226,716 bytes／640幅735,580 bytes（各14枚）。320幅は99.31%減、640幅は97.78%減。原本とのSHA256照合、縦横比、42派生画像と決定的変換の一致を確認。
- 元画像は新規配信物に含めず、public内の既知の重複だけを元画像と照合して除去。未知の内容・symlinkは削除せず停止。ローカル配信も元PNGを404にし、URLエンコードによる迂回を拒否。
- Cookieの改ざん・重複・期限・別Origin/Host、不正な転送IP、IPv4-mapped IPv6とIPv6/64の正規化を確認。
- Turnstileはホスト・action・送信ID・有効期限をサーバーで検証。キー不足、無効・使用済みproof、障害、過大入力ではAIを呼ばない。サーバー検証は模擬応答で試験し、本物のCloudflare接続は未実施。
- 006を追加・再適用し、既存Google/有料の残高と権限を維持。guestの生成予約は共通日次3回、回線6新規予約/分・30/日、全体/無料費用予算を一つのトランザクションで検査。拒否時の新規guest作成もロールバック。
- 同じ送信IDの再確認、別IDでの同proof競合、第三者の会話ID、無料枠終了、既知失敗、結果不明、JST日付またぎ、10分後の最大原価精算と旧IDの終了をAPI→実SQL互換環境で確認。
- 匿名Cookieでは購入・契約・Google履歴のAPIを利用不可。Google/匿名切替時は表示会話・コピー用スナップショットを破棄。開始処理・古い返答・履歴取得の遅延応答はowner/epochで無効化。
- 初回公開閲覧ではconfigを共有し、packages・guest作成・Turnstile・Google SDKを不要に呼ばない。匿名開始・新規送信のみボット判定を呼び、キャンセル時は勝手に生成しない。

ChromeのPC・スマホで、利用確認の中止・失敗ではsession/chat POSTなし、成功した3往復だけ3回の生成POST、4回目のフォーム送信でも追加POSTなし、3回目の返答と持ち帰りボタンの表示を確認。Cookieを保持した再読込後に残り2往復が維持され、sessionを再作成しないこと、人格コピー用ダイアログも確認しました。画像はWebPを配信し、PNGをページから要求しません。

PGliteは単一接続のため、複数接続による本番負荷試験を代替しません。実OpenAI/Stripe課金、Google OAuth完走、本番Turnstile、クラウドDB移行、Vercelデプロイ、Firewall公開は未実施です。設定前の匿名生成はfail-closed、人格コピーは維持します。生成以外のFunction/DB/配信負荷や分散攻撃を完全には防ぎません。

## 前回：人格持ち帰り・Googleアカウントの日次3往復

販売版の `npm test` は110件成功。`npm run check`、`npm run build` も成功。以下の旧ソクラテス初回枠・料金の記録は過去の検証であり、現在の提供条件ではありません。

- 005を追加し、003/004を変更せず、Googleアカウントごとの日本時間日別台帳へ移行。旧残高・契約・旧未確定リクエストは維持。
- JST 0時の境界、全14人で3回共有、送信IDの再確認、予約日の固定、日付またぎの成功・結果不明・復旧、旧004枠の精算を実SQL互換環境で確認。
- 総予算3ドル/日・30ドル/月、無料内枠0.50ドル/日・5ドル/月を維持（UTC基準）。無料枠停止時の購入分への自動切替を拒否。
- 新規CheckoutはSALES_ENABLEDの既定値falseで停止。既存購入分の利用・購入反映・契約管理を維持。
- 14人格の公開データを正本から生成し、API専用応答契約・内部メモを除外。人格だけなら個人情報なし、ログイン・生成API不要。
- 会話同梱は初期OFFで、明示選択時の表示中の発言だけ。長さ制限と省略表示、引用のエスケープ、秘密情報・状態・別履歴の非同梱を確認。
- クリップボード成功後のみ成功表示。拒否・未対応・非セキュア時は選択と手動コピー。ChatGPTは固定URLの明示リンクで、自動送信なし。
- ログアウト・アカウント変更時は会話入りプレビューと表示中の会話を破棄。旧アカウントの遅い応答が新アカウントに表示されない回帰試験を追加。
- 操作計測は固定カテゴリのみのページ内カウンタ。外部送信・サイト全体の集計は未導入。
- 端末の時計ずれや古い更新時刻で1秒ごとの残数確認を繰り返さないよう、過去の更新時刻には5分の再確認間隔を設ける。端末時計だけで無料回数を付与しない。
- 公開用持ち帰りモジュールは配信でき、SQL・サーバーソース・環境変数ファイルは404。

ChromeのPC・スマホ表示で、未ログインのコピー成功、権限拒否を模擬した手動選択（偽の成功表示なし）、会話同梱の初期OFF・任意選択、14カードの表示を確認。仮の認証/APIでプラトン1往復＋ニーチェ2往復を行い、合計3で残0、3回目の応答を保持、入力停止、持ち帰り導線を確認。4回目のフォーム送信操作でも `/api/chat` は累計3回のまま。無料対話の受付OFF・匿名でも持ち帰り導線は維持。ページ例外・コンソールエラー・横はみ出しはなし。ChatGPTへの実遷移・本文送信は行わず、リンクの固定URL/rel/明示クリックは自動テストで確認しました。

実OpenAI・Stripe課金は発生させていません。クラウドへの反映、Google OAuth完走、本番DB・契約照合は未実施です。PGliteによる予約の検証は、複数接続の本番負荷試験の代替ではありません。

## ソクラテス初回3往復の追加検証

販売版の `npm test` は73件成功（API・実SQL・人格・UI・静的配信・採算計算）。`npm run check` と `npm run build` も成功。

- 003を変更せず004を追加、再適用しても購入灯火・旧残高・無料消費済み回数をリセットしない。
- Google識別ハッシュの一意性、ソクラテス3回、他13人の有料限定、4回目停止。
- 正常保存時のみ無料回数を消費し、購入灯火と予約元を分離。
- 無料予算と全体予算、明確な失敗・結果不明・遅延復旧・同一ID再送を確認。
- 別タブで無料残数が変わる競合は、送信時の無料／有料の意図をfingerprintに固定し、DBロック下で409停止。勝手に購入灯火へ切り替えない。
- 旧形式の未確定送信IDの再確認も維持。
- 採算表は本番価格定義を読み込む `scripts/profit-scenarios.mjs` で再計算し、150円／ドルと180円／ドルの仮定でテスト。

今回の無料体験試験に実OpenAI通信・実決済は使っていません。契約プランの変更・本番DB移行・公開もしていません。

Chromeの1440×1000／390×844で仮の認証・APIを使い、無料残3→2→1→0、購入分0維持、4回目の送信リクエストなし、再読込でも復活しないこと、他賢者の有料限定、受付停止時に無料CTAが消えることを確認。終了時も3回目の返答が読め、購入案内は1個。ページ横はみ出し・例外・コンソールエラー・通信失敗なし。実OAuthや本番接続を通した試験ではありません。

## 自動テスト

販売版：API、PGliteによる実SQL、人格、UI、静的配信のテスト。
個人版：APIと永続送信台帳、入力検証、再試行UIのテスト。

`npm test` はOpenAI／Stripeの実通信を行いません。SQLは本番DBではなく隔離したPGliteで実行し、migration再適用、残高・予算予約、二重付与防止、遅延復旧、権限拒否、保存失敗のロールバックを確認しました。
PGliteは単一接続なので、複数DB接続での高負荷競合試験を代替しません。

## 実モデル

最終設定：gpt-5.4-mini、reasoning low、standard、最大出力900。
全14人への同じ哲学的質問＋ソクラテス・ニーチェ・ブッダへの反論と定義変更、合計20応答を実送信。

- 20/20で構造化応答を正常取得。最大出力501トークン（メモ・内部推論込み）。
- 合計API原価0.05339025ドル、1応答平均約0.00267ドル。
- 入力数確認を含む平均約2.90秒。ネットワーク・時間帯で変化する参考値。
- 3人ともユーザーの満足の定義変更を返答とメモに反映。
- 初回比較で人格差が弱かった点を補強してから再試験。

これらは短い架空対話での値です。長い会話の平均費用や応答時間の保証ではありません。
論理の厳密さ、思想史への忠実さ、提案と合意・回答と解決の厳密な区別には引き続き評価が必要です。対話メモもAI生成であり、完全な記憶とは扱いません。

## 実ブラウザ

Chromeの1440×1000と390×844で、一覧・料金を確認。
14カード、画像欠損なし、スマホの横はみ出しなし。
100円40返答／300円140返答／1,000円500返答、未ログイン時の案内を確認。
ページ例外・コンソールエラーなし。匿名閲覧でのAPIアクセスはconfig/packagesのGETのみ。

## 未実施

本番Supabaseのmigration・データ移行、実OAuth完走、Stripe test/liveの通し決済、既存契約の本番照合、Vercelへのデプロイ、WAF設定、複数接続による負荷試験、法務確認。
公開条件はDEPLOYMENT.mdを参照。
