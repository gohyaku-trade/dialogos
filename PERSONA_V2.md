# 人格プロンプト改訂 2026-09-22

人格をコピーして自分のChatGPTへ持ち帰る体験を維持する改訂。
無料枠、認証、会話同梱の初期OFF、クリップボード失敗時の手動コピー、
固定のChatGPTリンク、モデル・生成予算・DBスキーマは変更しない。

## 正本と配布

- `shared/persona-foundations.js`: 全14人格の対象史料、価値の優先順位、訂正可能な適用、未合意の前提、近い思想との違い。各2〜3件の一次資料へのリンク。
- `shared/persona-voices.js`: 全14人格の連続した二往復の創作例。文体だけでなく、判断への反論を受けて根拠を説明・修正する例。
- `shared/dialogue.js`: 上記をAPIと持ち帰りの両方へ合成。思想の説明と現代への応用を区別し、反射的な同意・反対・折衷を抑える。問いが添えられたコピーでは初回挨拶で問い直さない。
- `scripts/build-public.js`: 公開用 `js/data/takeawayPersonas.js` を生成。TAKEAWAY_VERSION=2、TAKEAWAY_REVISIONで版を識別。

持ち帰り版に原典の参照箇所とURLを含む。API版は同じ思想上の指示と参照箇所を保持し、URLだけ省いて入力を節約する。
典拠一覧は検索結果や原文そのものではない。引用したと偽らず、史実の本人・霊・啓示・私的記憶を装わない。
「慈悲と誠実の案内人」は引き続き預言者本人の再現ではない。

## 対話の継続

220字の既存状態形式は維持する。訂正を適用した後、未解決の問い、定義、現在の主張と理由、必要な事実を優先し、訂正の履歴と解決済みの話題は残りの枠に保存する。
古い解決済み事項に押されて現在の反論が失われる問題を回帰テストで確認する。
コピー先には内部状態を含めず、利用者が選んだ表示中の会話のみ従来どおり引用資料として添付する。

## 検証と限界

`npm run build`、`npm run check`、`npm test` でコピー機能、生成物の一致、状態圧縮、公開範囲、既存API・無料枠を検証する。
文字数ガードはトークン数の保証ではなく、API経路では従来どおりプロバイダーの入力トークン数を確認して6,000以内に制限する。

`npm run eval:personas` は既定で実行計画のみを表示し、資格情報を読み込まずAPIも呼ばない。
`LIVE_DIALOGUE_QA=1` と検証用の `DIALOGOS_QA_ENV_PATH` を明示した場合だけ、合成質問で最大0.30 USD（現行モデルの計画単価）の予約上限まで呼ぶ。
任意の `QA_BASELINE_PATH` に以前の公開人格モジュールを指定すると、同じ質問による旧版との比較を加える。
API14人格、代表4人格の追問、代表4人格のユーザーロールでの持ち帰り模擬、旧版模擬の計最大34呼び出し。
完全なリクエスト、プロンプトSHA256、モデル、使用量、返答を時刻付きのローカル `qa-output/` に保存する。実ユーザーの会話は使用せず、秘密値は保存しない。

本改訂時は、対象プロジェクトからローカル検証に使えるOpenAI APIキーを取得できず、ライブ比較は開始前に停止した（生成呼び出し0）。
オフライン検査の成功を、実モデルでの忠実度向上やRLHFの上書きの証明とは扱わない。
APIのユーザーロールで行う持ち帰り模擬も、ChatGPT製品そのものの検証ではない。

## Git履歴との同期

作業開始時、公開中のv3ソースはローカルの `dialogos-v3` にあり、Vercel接続先GitHubのmainは旧v2（51c58ca）だった。
Git経由の反映で公開済みの持ち帰り・匿名試用等を巻き戻さないため、公開中v3の構成も同時に履歴へ揃える。
環境変数、一時QA結果、Vercel認証情報、node_modules、生成publicディレクトリはコミット対象外。
